/**
 * Which worlds this build shows, in tab order (see ROADMAP.md — pacing is
 * controlled here, content is never deleted). Comment a line out to retire
 * or stage a world for a given deployment.
 */
export const VISIBLE_WORLDS: string[] = [
  'dino-valley',
  'star-station',
  'whisker-woods',
  'hiragana-island',
  'tenten-island'
]
